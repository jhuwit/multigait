% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_multigait}
\alias{run_multigait}
\title{Run a MultiGait pipeline}
\usage{
run_multigait(data, pipeline = multimorbidity_impaired_pipeline(), safe = TRUE)
}
\arguments{
\item{data}{A dataset created by \code{\link[=as_multigait_data]{as_multigait_data()}} or a compatible
Python dataset object.}

\item{pipeline}{A MultiGait pipeline, by default the
\code{\link[=multimorbidity_impaired_pipeline]{multimorbidity_impaired_pipeline()}}.}

\item{safe}{If \code{TRUE}, use the upstream \code{safe_run()} method.}
}
\value{
A named list containing \code{pipeline}, \code{gait_sequences},
\code{initial_contacts}, \code{per_second}, \code{per_stride}, \code{per_walking_bout}, and
\code{aggregated} results. Data frames are converted to R.
}
\description{
Run a MultiGait pipeline
}
\examples{
\dontrun{
recording <- as_multigait_data(wrist_imu, sampling_rate_hz = 100)
run_multimorbidity_impaired_pipeline(recording)
}
}
